services:
  - type: worker
    name: discord-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python start_render.py
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: AIML_API_KEY
        sync: false
        
